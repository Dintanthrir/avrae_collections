List available classes and subclasses configured in `voluble_config` server, user, and character variables.

Lists content from the current character's csettings version by default. Specify `-version 2014`, `-version 2024`, or both to choose list content.
Search for matching craftable magic items.

```
!craft search magic -type <category> -item <search term> [-rarity <rarity]
```

`-rarity` can be specified more than once to restrict results to _any_ of the given rarities.

__*Example*__
> `!craft search -type wondrous -item "Robe of Useful Items"`
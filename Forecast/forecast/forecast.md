Generates a weather report with forecasts for every visible location.

Locations are visible if they have no restrictions or are limited to a channel or category which matches the location where the alias was run.

Forecasts are stable and will be the same for all players running `!forecast` during a period configured in the `forecast_settings` svar.